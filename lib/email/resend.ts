// Placeholder for Resend integration.
// Add transactional email sending here when reservation notifications are built.
export async function sendEmail() {
  return { ok: false, message: 'Email provider not configured yet.' };
}
